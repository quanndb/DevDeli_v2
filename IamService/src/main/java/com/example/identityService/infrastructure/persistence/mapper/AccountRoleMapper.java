package com.example.identityService.infrastructure.persistence.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface AccountRoleMapper extends CustomAccountRoleMapper{
}
